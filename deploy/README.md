# API → VPS + PM2 + Nginx → https://api.sizor.online
#
# DNS: A → IP del VPS
#   chmod +x deploy/deploy.sh && ./deploy/deploy.sh
# Nginx: deploy/nginx/api.sizor.online.conf
# Guía: repo front → deploy/DEPLOY.md
